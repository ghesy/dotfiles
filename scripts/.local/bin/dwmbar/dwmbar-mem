#!/bin/sh
echo 
eval "$(head /proc/meminfo | tr : = | tr -d '( )' | rev | cut -c3- | rev)"
percentage=$((100*(MemTotal-MemAvailable)/MemTotal))
[ $percentage -lt 80 ] && echo  && exit
echo $percentage%
