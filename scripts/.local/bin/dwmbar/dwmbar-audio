#!/bin/sh
glyph="${XDG_RUNTIME_DIR:-/tmp}/dwmbar/audio_glyph"

if [ ! -r "$glyph" ] || [ -z "$(cat "$glyph")" ]; then
    sleep 2
    pacycle -g
fi

cat "$glyph"
pamixer --get-volume-human
mpc | grep -qi '^\[playing' && echo 
