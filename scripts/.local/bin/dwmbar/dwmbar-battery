#!/bin/sh
echo 
for battery in /sys/class/power_supply/BAT*; do
    cat $battery/capacity | tr -d '\n'
    echo %
    case $(cat $battery/status) in
        Charging) echo + ;;
        Full) echo  ;;
    esac
done
