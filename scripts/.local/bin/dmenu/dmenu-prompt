#!/bin/sh
# Ask the user
ans="$(printf "No\\nYes" | dmenu -p "$1" ${WINDOWID:+-w $WINDOWID} -nb darkred -sb maroon -sf white -nf darkgray)" || exit 1
[ "$ans" != Yes ] && exit 1
[ -z "$2" ] && exit 0
exec sh -c "$2"
