#!/bin/sh
# Show some options to launch some mounting scripts with dmenu.

# Config
prompt='Mount What?'

actions="
 Bashmount : exec $TERMINAL -e bashmount,
ﴓ FTP : exec $TERMINAL -e mftp,
 ADB : exec $TERMINAL -e madb,
"

eval "$(choices "$prompt" "$actions")"
