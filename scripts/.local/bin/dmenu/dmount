#!/bin/sh
# Show some options to launch some mounting scripts with dmenu.

# Config
prompt='Mount What?'

actions="
 Bashmount : $TERMINAL -e bashmount,
ﴓ FTP : $TERMINAL -e mftp,
 ADB : $TERMINAL -e madb,
"

eval "$(choices "$prompt" "$actions")"
