#!/bin/sh
# cycle the audio output device of pulseaudio.
pactl set-default-sink "$(pacmd list-sinks | grep index: -A1 | tr -d '\n' |
    tr '>' '\n' | grep -v aloop | sed '$aindex: 0' | grep -A1 ' \* ' |
    sed '1d' | grep -Po 'index: \K\d+')"
