#!/bin/bash
# toggle the currently playing players.
f=${XDG_RUNTIME_DIR:-/tmp}/toggleall
p="$(playerctl -a status -f '{{playerInstance}}	{{status}}' | grep Playing)"
if [ -n "$p" ]; then
    printf %s "$p" >$f
    playerctl -a pause
else
    cut -f1 $f | xargs -rL1 playerctl play -p
fi
