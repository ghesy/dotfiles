# vim:ft=sh
root() {
    exec sudo -A "$@"
}

tui() {
    exec ${TERMINAL:?} -e "$@"
}

tuiroot() {
    exec ${TERMINAL:?} -e sudo -A "$@"
}

web() {
    exec ${BROWSER:-xdg-open} "$@"
}

fixfonts() {
    ( ( sleep 2 && fc-cache -f ) & )
    exec "$@"
}
