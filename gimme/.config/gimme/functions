# vim:ft=sh
su()    { exec sudo -A "$@" ;}
tui()   { exec $TERMINAL -e sh -c "$@" ;}
tuisu() { exec $TERMINAL -e sudo -A "$@" ;}
web() {
    case "$BROWSER" in
        *fox*|*wolf*) exec "$BROWSER" --new-window "$@" ;;
        *) "$BROWSER" "$@" ;;
    esac
}
