# vim:ft=sh
root() {
    exec sudo -A "$@"
}

tui() {
    exec $TERMINAL -e sh -c "$@"
}

tuiroot() {
    exec $TERMINAL -e sudo -A "$@"
}

web() {
    case "$BROWSER" in
        *fox*|*wolf*) exec "$BROWSER" --new-window "$@" ;;
        *) "$BROWSER" "$@" ;;
    esac
}
